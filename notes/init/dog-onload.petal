function $onload() {
	// This will get printed so we can see that it loaded.
	$.log("dog.onload called");

	// If the dog was released, kick-start his tick handler.
	if (this.released)
		this._tick();
}
