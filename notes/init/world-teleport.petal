var teleport = {
	sigs: [ "teleport self", "teleport none at self" ],
	code: function() {
		$player.$hear(["Poof! You teleported into ", $.notate(this)]);
		$.move($player, this);
		this.look();
	}
};
